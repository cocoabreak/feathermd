# 実装タスク: WebView2自動スモークテスト

凡例: `[ ]` 未着手 / `[x]` 完了 / `[-]` 対象外・スキップ

## T-001: CDP共通ドライバー

- [x] 起動・接続・評価・条件待機・スクリーンショット・終了処理を共通化する
- [x] 空きポートと専用WebViewプロファイルを利用できる
- [x] 既存 `run-feathermd` CLIを共通ドライバーへ移行する

## T-002: fixtureとスモークrunner

- [x] Markdown・Mermaid・大容量fixtureを一時生成する
- [x] 依存ゼロの最小ZIP writerを追加する
- [x] 7シナリオを順次実行し、失敗時に非0終了・スクリーンショット保存する
- [x] 成否にかかわらずプロセスツリーと一時fixtureを後始末する
- [x] debug限定でsingle-instanceを無効化し、既存の手動起動アプリとCDP接続が競合しないようにする
- [x] Windowsの一時ディレクトリ削除を再試行する

## T-003: dev限定E2Eフック

- [x] 大容量文書を同じ判定経路でセーフモードへ開くフックを追加する
- [x] ZIP内Markdownを開くフックを追加する
- [x] 更新あり状態の注入とセッションflushフックを追加する
- [x] productionビルドで利用できないことを検査する

## T-004: CI統合

- [x] `npm run e2e:smoke` を追加する
- [x] Windows CIへ本番フロントエンド・Tauriビルドジョブとキャッシュを追加する
- [-] GitHub hosted runnerでのWebView2スモーク実行（非対話環境ではCDPが開始されないため、ローカルWindowsのリリースゲートとして運用する）
- [x] ローカル実行の失敗時ログ・スクリーンショットをartifact化する
- [x] 一時成果物をgitignoreへ追加する

## T-005: 検証・レビュー

- [x] ローカルWindowsでスモークsuiteを完走する
- [x] frontend / Rustのformat・lint・check・testを通す
- [x] 設計・差分レビューとセキュリティレビューを完了する

## T-006: セッションシナリオの安定化

- [x] スクロール可能なDOMとストア反映を期限付きで待つ
- [x] 直接 `location.reload()` せず、アプリの `Ctrl+R` 保存・再読込経路を検証する
- [x] 旧ページ固有マーカーの消失を待ち、再読込前の状態を誤って成功扱いしない
