# 実装タスク: ドラッグ＆ドロップ

凡例: `[ ]` 未着手 / `[x]` 完了 / `[-]` 対象外・スキップ

## T-001: パス種別判定

- [x] Rustコマンド`stat_path`でパスの存在とファイル・フォルダー種別を返す
- [x] `stat_path`をTauri invoke handlerへ登録する
- [x] 各入力パスを`normalizePath`で正規化する

## T-002: 共通パス処理

- [x] 複数パスを順番に処理する`processPaths`を実装する
- [x] 最初のフォルダーだけを`openFolder`で開く
- [x] `.md`・`.markdown`ファイルだけをRust側で認可後に`openMarkdownFile`で開く
- [x] パス単位で例外を捕捉し、後続パスの処理を継続する
- [x] CLI引数処理とドラッグ＆ドロップで同じ処理を再利用する

## T-002A: セキュリティ境界とリソース制限

- [x] WebViewから任意パスを`register_root`へ渡して信頼範囲を拡張できない設計へ変更する
- [x] Markdownファイルの最大読込サイズをRust側で検証する
- [x] 1イベントで処理するパス数または新規タブ数に上限を設ける
- [x] 信頼登録・サイズ・件数の境界値テストを追加する

## T-003: ドラッグ＆ドロップイベント

- [x] `tauri://drag-drop`イベントをウィンドウ全体で購読する
- [x] イベントの`payload.paths`を共通パス処理へ渡す
- [x] 画面破棄時にイベントリスナーを解除する

## T-004: セッション復元との統合

- [x] ファイルまたはフォルダーを正常に開いた場合にセッション復元プロンプトを閉じる
- [x] 無効な入力だけの場合はプロンプトを維持する

## T-005: 検証

- [x] format・lint・Svelte/TypeScript check・フロントエンドテストを実行する
- [x] Rust format・Clippy・テストを実行する
- [x] Windows実アプリへTauriイベントを注入し、単一・複数Markdown、重複、フォルダー、非Markdown・存在しないパスの処理を確認する
- [x] Windows Explorerから単一・複数Markdownファイルを実際にドロップして確認する
- [x] Windows Explorerからフォルダーと混在入力を実際にドロップして確認する
- [x] 設計・差分レビューとセキュリティレビューを完了する
