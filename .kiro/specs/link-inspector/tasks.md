# 実装タスク: リンクインスペクターとローカルリンクグラフ

## 1. 共通リンク索引

- [x] 1.1 WikiリンクとMarkdownリンクを表すRustデータ型を追加する
- [x] 1.2 Wikiリンク抽出を見出し・生ターゲットを保持する共通抽出処理へ拡張する
- [x] 1.3 `pulldown-cmark`からローカルMarkdown文書リンクを抽出する
- [x] 1.4 表示時遷移と一致するSource内Markdownリンク解決を実装する
- [x] 1.5 `BacklinkIndex`を双方向の`LinkIndex`へ置き換える
- [x] 1.6 `includeWikiLinks`を抽出条件とキャッシュキーへ追加する
- [x] 1.7 既存の負荷上限、キャッシュ、同時構築制限、watcher無効化を移行する
- [x] 1.8 Edgeの一重保持と1文書・索引・文字列総量・応答件数／容量上限を実装する
- [x] 1.9 `get_source_link_context`コマンドを登録する
- [x] 1.10 `list_source_backlinks`を互換アダプターへ移行し、不要になった時点で削除する
- [x] 1.11 区分ごとの`items`と`total: Option`で正確／件数不明の省略契約を実装する

## 2. 索引テスト

- [x] 2.1 Wikiリンクの既存解決規則とバックリンク互換テストを追加する
- [x] 2.2 Markdownのインライン・参照形式・見出し付きリンクをテストする
- [x] 2.3 外部URL、画像、生HTML、コード、非Markdown、Source外リンクの除外をテストする
- [x] 2.4 リンク切れ、重複参照、自己参照、Wiki／Markdown混在をテストする
- [x] 2.5 Native/ZIP、隠しファイル、gitignore、generation、TTL、上限到達をテストする
- [x] 2.6 100,000件の異なるリンク切れと長いanchorでメモリ・応答上限をテストする
- [x] 2.7 Wikiプラグイン無効／有効時の抽出結果とキャッシュ分離をテストする
- [x] 2.8 `rawTarget`のbroken限定契約と`total`のSome／Noneをテストする

## 3. リンクインスペクター

- [x] 3.1 `LinkInspectorStore`とTypeScript型を追加する
- [x] 3.2 既存BacklinksStoreのlatest-only、直列化、dirty、明示更新を移行する
- [x] 3.3 `LinkInspectorPanel.svelte`へ出力・入力・問題の一覧を実装する
- [x] 3.4 ファイル名、相対パス、種別、見出し、参照数、リンク切れ状態を表示する
- [x] 3.5 各区分500件の描画上限と省略件数表示を実装する
- [x] 3.6 Sidebarのバックリンクタブをリンクタブへ移行する
- [x] 3.7 watcher・Source変更・設定変更時の無効化を接続する
- [x] 3.8 日英文言とコンポーネント／ストアテストを追加する

## 4. ローカルリンクグラフ

- [x] 4.1 グラフ用の最大40ノード・安定したノード／エッジモデルを純粋関数で実装する
- [x] 4.2 入出力ノード統合、リンク切れ仮想ノード、省略件数を実装する
- [x] 4.3 Cytoscape.jsの同心円レイアウトでCanvasグラフを実装する
- [x] 4.4 シングルトンでリサイズ可能なリンクグラフWebViewウインドウを実装する
- [x] 4.5 現在文書・テーマ・言語・索引設定の追従と、再読み込みを跨ぐversion同期を実装する
- [x] 4.6 メイン／リンクグラフのcapability分離と用途限定のRust仲介コマンドを実装する
- [x] 4.7 ノード選択、キーボード操作、リンク切れ表示、メニュー非表示を実装する
- [x] 4.8 グラフモデルの上限・ノード統合・アクセシビリティ・逆順更新・古い遷移拒否の
      テストを追加する
- [x] 4.9 WebView2でNative/ZIPのグラフ表示、同心円配置、リサイズ、メニュー非表示、
      文書追従、ノード遷移を確認する

## 5. 完了検査

- [x] 5.1 `npm run format`、`npm run lint`、`npm run check`、`npm test`を実行する
- [x] 5.2 `cargo fmt`、`cargo clippy -- -D warnings`、Rustテストを実行する
- [x] 5.3 Windows WebView2スモークテストを実行する
- [x] 5.4 設計・差分レビューを実施し、P0〜P2を解消する
- [x] 5.5 ファイル・パス・HTML/SVGを対象とするセキュリティレビューを実施し、P0〜P2を解消する
