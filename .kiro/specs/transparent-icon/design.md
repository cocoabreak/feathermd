# 透過アイコン 技術設計

## ステータス

完了

## 方針

生成AIによる再描画は形状変化のリスクがあるため採用しない。既存原本から背景色をアルファ化し、ロゴ本体を保持した透過PNGを新原本とする。

新原本からTauri CLIの`icon`コマンドでプラットフォーム別アイコンを生成する。faviconは生成された128×128 PNGを利用する。

## 検証

- 原本・生成PNGの寸法、ピクセル形式、四隅alpha値を機械確認する。
- 32×32、128×128、原本を目視確認する。
- `npm run check`とTauri設定の参照先確認を行う。
