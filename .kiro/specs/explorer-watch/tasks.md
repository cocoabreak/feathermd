# 実装タスク: エクスプローラーのファイル増減ウォッチ (explorer-watch)

凡例: `[ ]` 未着手 / `[x]` 完了 / `[-]` 対象外・スキップ

---

## Phase 1: バックエンド (Rust)

### T-001: watch_directory / unwatch_directory コマンド

- **依存**: なし
- **完了条件**:
  - [x] NonRecursive監視でディレクトリ直下の変化を検知し、デバウンス後に `"directory-changed"`（payload=監視ディレクトリパス）を送出する
  - [x] ファイル監視（`watch_path`）とは独立に登録・解除できる（キー衝突なし。`DirWatcherState` を分離）
  - [x] 信頼済みルート配下でないパスの監視は拒否される（`AllowedRoots::resolve` で検証）
  - [x] ファイル100件・Explorer 65件の上限をRust側で強制する
  - [x] イベント分類のユニットテスト（`classify_event_kind` の既存テストを流用。デバウンス集約はE2Eで確認）
- **対応US**: US-001

## Phase 2: フロントエンド

### T-002: 監視ライフサイクル

- **依存**: T-001
- **完了条件**:
  - [x] Rust側の一括reconcileコマンドへ現在表示中の集合を送る
  - [x] フォルダを開く／開き直す／gitignoreトグル変更／WebViewリロード時に現在の集合へ収束する
  - [x] 同期要求を直列化し、未開始の古い世代をスキップする
- **対応US**: US-001

### T-003: directory-changedイベントの反映とマージ

- **依存**: T-002
- **完了条件**:
  - [x] イベント受信でそのディレクトリ1階層が再読込・差し替えされる
  - [x] 残存ディレクトリの展開状態・読み込み済み子孫が維持される
  - [x] 消えたディレクトリの展開状態・監視が後始末される
- **対応US**: US-001

## Phase 3: 検証

### T-004: E2E動作確認

- **依存**: T-001〜T-003
- **完了条件**:
  - [x] 実アプリで、表示中レベルでのファイル追加・削除・リネームが数秒以内に反映される
  - [x] gitignore対象の追加ファイルが（考慮ONのとき）表示されない
  - [x] 未展開ディレクトリ内の変化では何も起きない
  - [x] format / lint / check / vitest / cargo系がクリーン

### T-005: 監視資源の堅牢化

- [x] Rust側台帳を唯一の正とする `reconcile_directory_watches` を追加する
- [x] 追加失敗時に既存集合を変更しない
- [x] 監視上限の境界値と、同期中の状態変更が最新集合へ収束することを単体テストする
