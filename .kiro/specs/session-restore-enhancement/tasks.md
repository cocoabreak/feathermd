# 実装タスク: セッションUI状態の復元強化

凡例: `[ ]` 未着手 / `[x]` 完了 / `[-]` 対象外・スキップ

## T-001: セッションUIストアとスクロール復元

- [x] 表示モード別スクロール位置を保持するストアを追加する
- [x] `MarkdownViewer` のローカルMapをストアへ置き換える
- [x] 保存値の検証とストア単体テストを追加する

## T-002: Explorer展開状態の復元

- [x] 展開中パスを `tabs.json` へ保存する
- [x] 親から順に遅延読み込みする復元関数を追加する
- [x] 欠損・読み込み失敗・64件・累積10,000エントリ上限をテストする

## T-003: 検索状態の復元

- [x] 検索ストアに保存用スナップショットと復元操作を追加する
- [x] 開閉・検索語・正規表現設定を保存・復元する
- [x] 派生結果を復元時に初期化するテストを追加する

## T-004: セッション永続化への統合

- [x] `PersistedSession` / `PersistedTab` を新形式へ更新する
- [x] 保存時タブインデックスと復元後タブIDを介してスクロール位置を結び直す
- [x] 既存の復元確認・破棄・保存抑止フローを維持する
- [x] 保存トリガーに追加UI状態を含める

## T-005: 検証・レビュー

- [x] `npm run format` / `lint` / `check` / `test` を通す
- [x] リロード・完全再起動・復元・破棄を実アプリで確認する
- [x] 設計・差分レビューとセキュリティレビューを完了する

## T-006: 再読込直前の保存保証と現行スキーマ固定

- [x] `Ctrl+R` / `Cmd+R` / `F5` を捕捉し、tabs/recent/settingsのflush完了後に再読込する
- [x] `activePath` / `rootPath` の互換読込・保存・Rust許可キーを削除する
- [x] `explorer` の `SourceSpec` だけでnative/ZIPのExplorerを復元する
- [x] 実際の `Ctrl+R` 経路で検索・スクロール復元をWebView2スモークテストする
