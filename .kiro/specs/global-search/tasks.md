# 実装タスク: グローバル検索

> **注**: 2026-07-10のspecステータス整理で実態に合わせて更新。全タスク実装済み。設計時の想定に加え、正規表現・大文字小文字区別のトグル、`.gitignore`考慮（explorer-lazy-load spec連動）も実装されている。

- [x] Rust側に検索コマンドを実装（大文字小文字無視、高速な走査） → `search_in_directory`（`search.rs`）。大文字小文字区別・正規表現・隠しファイル・.gitignore考慮のオプション付き
- [x] フロントエンドに検索パネルコンポーネントを作成 → `GlobalSearchPanel.svelte`（サイドバーの検索タブ）
- [x] 検索実行時のUI（ローディングインジケーターなど）の実装 → `isSearching` によるスピナー表示・エラー表示・0件表示
- [x] 結果リストからファイルを開き、検索キーワードをページ内検索ステートに渡す処理の実装 → `globalSearchStore.openMatch()` がファイルを開き、`searchStore` へクエリ・正規表現モード・対象行を引き継いでハイライト＆ジャンプ
