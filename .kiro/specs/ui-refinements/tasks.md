# 実装タスク: UI細部改善（フォルダを閉じる・履歴削除・目次リサイズ） (ui-refinements)

凡例: `[ ]` 未着手 / `[x]` 完了 / `[-]` 対象外・スキップ

---

## Phase 1: フォルダを閉じる

### T-001: メニュー項目とコマンドの追加

- **依存**: なし
- **完了条件**:
  - [x] `menu.rs` のファイルメニューに `file.closeFolder`（ja/enラベル）が追加される
  - [x] `closeFolder()` アクションが `explorerStore.clear()` と監視解除を行い、コマンドに登録される
  - [x] サイドバーの空状態判定が `rootPath === null` になり、閉じた後に最近開いたフォルダー一覧が表示される
  - [x] 閉じた状態がセッション保存され、タブは影響を受けない
- **対応US**: US-001

## Phase 2: 履歴の個別削除

### T-002: recentStoreの削除メソッド

- **依存**: なし
- **完了条件**:
  - [x] `removeFile` / `removeFolder` が追加され、ユニットテストが通る
- **対応US**: US-002

### T-003: 削除ボタンUI

- **依存**: T-002
- **完了条件**:
  - [x] 最近開いたファイル・フォルダー両一覧で、行ホバー時に左端へ×ボタンが表示される
  - [x] クリックで該当項目のみ削除され、`saveRecent()` で永続化される
  - [x] i18nに削除ボタンの文言（ja/en）が追加される
- **対応US**: US-002

## Phase 3: 目次ヘッダーバーでのリサイズ

### T-004: resize-dragアクションの抽出

- **依存**: なし
- **完了条件**:
  - [x] `actions/resize-drag.ts` が新設され、`ResizeHandle` が同アクションを使う形にリファクタされる
  - [x] サイドバー幅・目次上端ハンドルの既存挙動（ドラッグ・ダブルクリック・ハイライト）が維持される
- **対応US**: US-003

### T-005: 目次ヘッダーバーへの適用

- **依存**: T-004
- **完了条件**:
  - [x] ヘッダーバーのドラッグで高さ比率が変わり永続化、ダブルクリックでデフォルトに戻る
  - [x] ✕ボタンはドラッグ起点にならずクリックが従来どおり動く
  - [x] 目次が空のときは無効
- **対応US**: US-003

## Phase 4: 検証

### T-006: テスト・動作確認

- **依存**: T-001〜T-005
- **完了条件**:
  - [x] format / lint / check / vitest / cargo fmt / cargo clippy がクリーン
  - [x] 実アプリで3機能の受け入れ条件を一通り確認
